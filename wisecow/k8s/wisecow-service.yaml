apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 4499        # service port (external)
      targetPort: 4499  # container port
  type: NodePort   # or LoadBalancer if using cloud provider

